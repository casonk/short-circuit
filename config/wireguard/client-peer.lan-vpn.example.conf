# short-circuit WireGuard lan-vpn client peer example
#
# Profile: wireguard-lan-vpn
# Import the finished file into the WireGuard app on your client device by QR
# code or file transfer after replacing every placeholder.

[Interface]
Address = 10.99.0.2/32
PrivateKey = <client-private-key>
# setup_wireguard.sh configures dnsmasq on the server to answer this address
# for private hostnames. Set --dns-hostname to match the name your services use.
DNS = 10.99.0.1

[Peer]
PublicKey = <server-public-key>
Endpoint = <wireguard-endpoint-hostname-or-ip>:51820
AllowedIPs = 10.99.0.0/24, <lan-subnet-cidr>
PersistentKeepalive = 25

# Replace <lan-subnet-cidr> with the real home LAN route, for example:
# 192.168.0.0/24
#
# This profile routes both the WireGuard tunnel subnet and the wider home LAN
# through the tunnel.
#
# LAN-wide access after connecting:
# SMB:   smb://<any-lan-host>
# HTTPS: https://<private-hostname>
# SSH:   ssh user@<any-lan-host>
